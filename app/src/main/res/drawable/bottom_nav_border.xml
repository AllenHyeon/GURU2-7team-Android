<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />  <!-- 배경색을 투명하게 설정 -->
    <stroke android:color="@android:color/black" android:width="10dp"/>  <!-- 가로선 색상을 검정색으로 설정 -->
    <size android:height="10dp" />  <!-- 선 두께 -->
</shape>